🧹 code health: remove unused read_file_safe function

🎯 **What:** Removed the unused `read_file_safe` function, its associated tests, and dependent constants (`MAX_FILE_SIZE`, `CWD_REALPATH`, `CWD_REALPATH_PLUS_SEP`) from `code_health_scanner.py`.

💡 **Why:** The function was defined but no longer referenced or called by any active code within the module or the broader codebase. Removing dead code improves maintainability, reduces cognitive load, and keeps the codebase clean.

✅ **Verification:**
- Traced usage with `grep` to confirm the function and its constants were completely unused.
- Adjusted imports and removed isolated tests for the deleted function in `tests/test_code_health_scanner.py`.
- Verified that all remaining tests run successfully with `pytest`.
- Ensured the primary logic within `scan_file` remains unaffected.

✨ **Result:** A cleaner codebase with reduced tech debt, preserving exact functional parity without unnecessary dead code.
