name: PR Validation

permissions:
  contents: read

on:
  push:
    branches: [main]
    paths:
      - "**.py"
      - "Series_27/Analysis/requirements.txt"
      - "requirements*.txt"
      - ".github/workflows/pr-validation.yml"
  pull_request:
    branches: [main]
    paths:
      - "**.py"
      - "Series_27/Analysis/requirements.txt"
      - "requirements*.txt"
      - ".github/workflows/pr-validation.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        if: matrix.python-version == '3.11'
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r Series_27/Analysis/requirements.txt
          python -m pip install -r requirements-dev.txt

      - name: Lint with ruff
        if: matrix.python-version == '3.11'
        run: |
          # Stop the build if there are Python syntax errors or undefined names
          ruff check --select=E9,F63,F7,F82 --target-version=py311 .
          # Exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
          ruff check --exit-zero --target-version=py311 .

      - name: Compile Python files
        run: |
          # SECURITY: Use -exec to securely handle file paths with spaces and prevent word splitting/command injection
          find . -name "*.py" -not -path "*/venv/*" -not -path "*/.venv/*" -exec python -m py_compile {} +

      - name: Run tests
        if: matrix.python-version == '3.11'
        run: |
          if [ -d tests ]; then
            python -m pytest tests/ -v || echo "::warning::Some tests failed"
          else
            echo "::notice::No tests directory found, skipping pytest"
          fi

      - name: Check for pandas OOM issues
        run: |
          # Verify MAX_FILE_SIZE constants are present in analysis scripts
          grep -r "MAX_FILE_SIZE" Series_*/Analysis/*.py || echo "::warning::No MAX_FILE_SIZE limit found in analysis scripts"
